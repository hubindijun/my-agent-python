# ============================================
# My RAG Project Dependencies
# Environment: conda - mylearn
# Python: 3.11
# ============================================

# ---------- Core Framework ----------
langchain>=0.3.0
langchain-core>=0.3.0
langchain-openai>=0.2.0
langchain-huggingface>=0.1.0
langchain-chroma>=0.1.0
langchain-text-splitters>=0.3.0
langgraph>=0.2.0
langfuse>=2.0.0

# ---------- LLM & Embedding ----------
openai>=1.40.0
sentence-transformers>=3.0.0
transformers>=4.40.0
torch>=2.2.0

# ---------- Vector Database ----------
chromadb>=0.5.0

# ---------- Web Server ----------
fastapi>=0.115.0
uvicorn>=0.30.0
pydantic>=2.8.0
pydantic-settings>=2.4.0

# ---------- MCP Integration ----------
# langchain-mcp-adapters 0.1.x 依赖 mcp 1.x，与 mcp 2.x API 不兼容
langchain-mcp-adapters>=0.1.0,<0.2.0
mcp>=1.0.0,<2.0.0
# cryptography 是 mcp -> pyjwt 的传递依赖，50+ 版本需要 OpenSSL 3.2+，
# 与常见系统/conda 环境的 OpenSSL 3.0 不兼容，锁定在兼容范围内
cryptography>=41.0.0,<43.0.0

# ---------- Utilities ----------
python-dotenv>=1.0.0
httpx>=0.27.0          # 调用 LiteLLM Proxy API（动态获取模型列表等）

# ---------- Optional (Development) ----------
# ipykernel>=6.29.0
# jupyter>=1.0.0
# pytest>=8.0.0
